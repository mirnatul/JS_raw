age = 27;
let voteable = (age < 18) ? "if condition true" : "if condition false";
// console.log(voteable);


let age = 27;
let category = (age < 13) ? "Child"
    : (age < 20) ? "Teenager"
        : (age < 60) ? "Adult"
            : "Senior";
console.log(category);
